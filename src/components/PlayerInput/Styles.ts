import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputWrapper: {
        flexDirection: "row",
        alignItems: "center",
        backgroundColor: "#fff",
        paddingHorizontal: 14,
        height: 55,
        borderRadius: 40,
        flex: 1,
    },

    icon: {
        width: 20,
        height: 20,
        marginRight: 10,
    },

    input: {
        flex: 1,
        fontSize: 18,
        fontWeight: "600",
    },

})