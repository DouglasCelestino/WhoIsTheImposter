import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#fff",
    },

    input: {
        height: 40,
        margin: 6,
        borderWidth: 1,
        padding: 10,
        borderRadius: 15
    },
})