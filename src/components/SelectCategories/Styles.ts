import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    Button: {
        width: "100%",
        height: 50,
        borderRadius: 20,
        alignItems: "center",
        justifyContent: "center"
    },
    
    active: {
        backgroundColor: "#FEE63C"
    },

    inactive: {
        backgroundColor: "#FFF"
    },
})